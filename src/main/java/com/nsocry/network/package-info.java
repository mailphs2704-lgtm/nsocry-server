/**
 * Owns TCP listener lifecycle, socket configuration, bounded session dispatch and
 * the adapter that connects a socket to the NSOCry session bootstrap.
 *
 * <p>This package must not contain gameplay or database logic.</p>
 */
package com.nsocry.network;
