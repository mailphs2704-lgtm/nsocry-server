/**
 * Cung cấp các bộ điều hợp quan sát runtime đã làm sạch dữ liệu trước khi ghi log.
 *
 * <p>Package này không được ghi password, token, payload client hoặc exception message không tin cậy.</p>
 */
package com.nsocry.observability;
