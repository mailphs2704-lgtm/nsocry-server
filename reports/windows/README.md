# Báo cáo Windows

Thư mục này nhận báo cáo do `NSOCRY_WORK.bat` tạo:

- `latest.md`: kết quả gần nhất để AI đọc nhanh.
- `history/<timestamp>-<commit>.md`: lịch sử bất biến theo lần chạy.

Không đặt log Maven đầy đủ ở đây. Log đầy đủ chỉ giữ cục bộ trong `.nsocry-work/`.
